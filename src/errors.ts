import * as errors from "@superbuilders/errors"

export const ErrTemplateNotValidated = errors.new("template not validated")

export const ErrTemplateExecutionFailed = errors.new(
	"template execution failed"
)
